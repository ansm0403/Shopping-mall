PROTOCOL=http
HOST=localhost:4000
POSTGRES_USER=your-username
POSTGRES_PASSWORD=your-password
POSTGRES_DB=your-database
PORT=4000

# JWT 설정
JWT_SECRET=your-jwt-secret-key-here
JWT_REFRESH_SECRET=your-jwt-refresh-secret-key-here

# Redis 설정
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=123123

# 이메일 설정 (Gmail 또는 Naver 중 선택)

# === Gmail 사용하는 경우 ===
# 1. Google 계정에서 2단계 인증 활성화
# 2. 앱 비밀번호 생성 (https://myaccount.google.com/apppasswords)
# 3. 생성된 앱 비밀번호를 MAIL_PASSWORD에 입력
# MAIL_HOST=smtp.gmail.com
# MAIL_PORT=587
# MAIL_SECURE=false
# MAIL_USER=your-email@gmail.com
# MAIL_PASSWORD=your-app-password

# === Naver 메일 사용하는 경우 ===
# 1. 네이버 메일 로그인
# 2. 환경설정 > POP3/IMAP 설정 > IMAP/SMTP 설정 사용 체크
# 3. 실제 네이버 계정 비밀번호 사용
MAIL_HOST=smtp.naver.com
MAIL_PORT=587
MAIL_SECURE=false
MAIL_USER=kirianir
MAIL_PASSWORD=WN7MFGU5V6ZM

# 공통 설정
MAIL_FROM_NAME=Shopping Mall
MAIL_FROM_ADDRESS=kirianir@naver.com

# 프론트엔드 URL
FRONTEND_URL=http://localhost:3000
